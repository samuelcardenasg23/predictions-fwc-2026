# Database
DATABASE_URL=postgresql://user:pass@localhost:5432/quiniela

# Auth
JWT_SECRET=change-me
JWT_EXPIRATION=7d

# App
PORT=3001
FRONTEND_URL=http://localhost:3000

# Football API
FOOTBALL_DATA_KEY=

# Email — choose provider: smtp (local/dev) | brevo (production on Render)
EMAIL_PROVIDER=smtp

# SMTP (Office 365) — used when EMAIL_PROVIDER=smtp
SMTP_HOST=smtp.office365.com
SMTP_PORT=587
SMTP_USER=tu-cuenta@flitsas.com
SMTP_PASS=
SMTP_FROM=Quiniela FWC 2026 <tu-cuenta@flitsas.com>

# Brevo — used when EMAIL_PROVIDER=brevo
BREVO_API_KEY=
BREVO_SENDER_EMAIL=tu-cuenta@flitsas.com
BREVO_SENDER_NAME=Quiniela FWC 2026
